[project]
name = "madefor.cc"
version = "0.1.0"
description = ""
authors = [
  { name = "Jonathan Coates", email = "git@squiddev.cc>" }
]

requires-python = ">=3.10"
dependencies = [
  "requests >=2.28.1,<3.0"
]

[tool.poetry]
package-mode = false

[tool.poetry.group.dev.dependencies]
mypy = "^1.15.0"
types-requests = "^2.28.9"

[tool.mypy]
python_version = "3.10"
